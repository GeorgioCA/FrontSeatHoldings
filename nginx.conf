server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 512;
    gzip_types text/html text/css text/javascript application/javascript application/json image/svg+xml;

    # Favicon / robots — no log noise
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
    location = /robots.txt {
        log_not_found off;
        access_log off;
    }

    # Health check — lightweight
    location = /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }

    # Brand assets — long cache
    location /brand/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Image assets — long cache
    location /img/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # HTML — no cache
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, must-revalidate";
    }

    # SVG / fonts — medium cache
    location ~* \.(svg|woff2?|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # Redirect old French careers URL
    location = /carrieres {
        return 301 /fr/carrieres;
    }

    # Main site — clean extensionless URLs
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    # Custom 404
    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
